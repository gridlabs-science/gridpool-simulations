# Simulation Reports

Generated reports should be written under `reports/generated/`.

That directory is ignored by git so long local sweeps do not pollute commits.
Promoted reports that support public claims can be copied into this directory as
stable markdown files once reviewed.

